import { h } from "preact";
import style from "./style.css";

const All = () => (
  <div class={style.home}>
    <h1>All</h1>
    <p>Use as many of the calculations as you need</p>
  </div>
);

export default All;
